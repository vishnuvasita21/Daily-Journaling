<?xml version="1.0" encoding="utf-8"?>
<!-- activity_journal.xml -->

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".JournalActivity">

    <!-- Add a SearchView -->
    <SearchView
        android:id="@+id/searchView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/buttonAddTag"
        android:queryHint="@string/search_hint" />

    <!-- Add an ImageButton for adding a new tag -->
    <ImageButton
        android:id="@+id/buttonAddTag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:src="@drawable/add"
        android:contentDescription="@string/add_tag"
        android:background="?attr/selectableItemBackgroundBorderless" />

    <!-- RecyclerView for displaying the list of journals -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/searchView"
        android:visibility="visible" /> <!-- Changed from 'gone' to 'visible' -->

</RelativeLayout>
